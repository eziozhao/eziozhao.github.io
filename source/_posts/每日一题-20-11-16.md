---
title: 每日一题 20/11/16 406.根据身高重建队列
tags:
  - 每日一题
  - leetcode
date: 2020-11-16 15:18:01
---
## 题目

假设有打乱顺序的一群人站成一个队列。 每个人由一个整数对`(h, k)`表示，其中`h`是这个人的身高，`k`是排在这个人前面且身高大于或等于`h`的人数。 编写一个算法来重建这个队列。

<!--more-->

**注意：** 总人数少于1100人。

**示例**

```
输入:
[[7,0], [4,4], [7,1], [5,0], [6,1], [5,2]]

输出:
[[5,0], [7,0], [5,2], [6,1], [4,4], [7,1]]

```

## 思路

- 按照身高下降的顺序对原数组进行排序，如果身高相同，则把k值大的放在后面，因为他前面站更多的人
- 遍历排序后的数组，对于每一个元素，其k值等于i，就让他放在新数组第 i 个位置( i 从0开始）。这样就可以保障每一个元素（h,k)的前面都有k个身高大于等于他的元素

## 代码

```java
class Solution {
        public int[][] reconstructQueue(int[][] people) {
            Arrays.sort(people, new Comparator<int[]>() {
                @Override
                public int compare(int[] o1, int[] o2) {
                    //身高大的放在前面
                    if (o1[0] != o2[0]) {
                        return o2[0] - o1[0];
                    }
                    //身高相同时 k值大的放后面
                    return o1[1] - o2[1];
                }
            });
            int len = people.length;
            List<int[]> ans = new ArrayList<>();
            for (int[] arr : people) {
                ans.add(arr[1], arr);
            }
            return ans.toArray(new int[len][]);
        }
    }
```