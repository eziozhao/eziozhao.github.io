---
title: 每日一题 20/11/13 328.奇偶链表
tags:
  - 每日一题
  - leetcode
abbrlink: 5ab9407b
date: 2020-11-13 10:51:55
---
## 题目

给定一个单链表，把所有的奇数节点和偶数节点分别排在一起。请注意，这里的奇数节点和偶数节点指的是节点编号的奇偶性，而不是节点的值的奇偶性。

请尝试使用原地算法完成。你的算法的空间复杂度应为 O(1)，时间复杂度应为 O(nodes)，nodes 为节点总数。

<!--more-->

**示例 1:**

```
输入: 1->2->3->4->5->NULL
输出: 1->3->5->2->4->NULL

```

**示例 2:**

```
输入: 2->1->3->5->6->4->7->NULL 
输出: 2->3->6->7->1->5->4->NULL
```

**说明:**

- 应当保持奇数节点和偶数节点的相对顺序。
- 链表的第一个节点视为奇数节点，第二个节点视为偶数节点，以此类推。

## 思路

- 遍历过程中修改指针指向，其实就是将指针隔位相连
- 注意链表长度为偶数时，奇元素指针要避免空指针异常

## 代码

```java
class Solution {
        public ListNode oddEvenList(ListNode head) {
            if (head == null || head.next == null) {
                return head;
            }
            //存储偶数链表的头节点
            ListNode second = head.next;
            ListNode i = head;
            ListNode j = head.next;
            //分别连接奇偶元素
            while (j != null) {
                i.next = j.next;
                //防止链表长度为偶数时产生空指针异常
                if (i.next != null) {
                    i = i.next;
                }
                j.next = i.next;
                j = j.next;
            }
            //将俩个部分拼接
            i.next = second;
            return head;
        }
}
```